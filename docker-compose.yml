services:
  ### JUPYTER ###

  jupyter-lab:
    build:
      context: ./docker/jupyter-lab
      args:
        UV_SYNC_OPTIONS: "--locked"
    container_name: jupyter-lab
    environment:
      MLFLOW_TRACKING_URI: ${MLFLOW_TRACKING_URI}
      PREFECT_API_URL: ${PREFECT_API_URL}
      MINIO_ROOT_USER: ${MLFLOW_MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MLFLOW_MINIO_ROOT_PASSWORD}
      MINIO_ENDPOINT_URL: ${MLFLOW_S3_ENDPOINT_URL}
    ports:
      - "127.0.0.1:8888:8888"
    volumes:
      - ./01-intro:/app/01-intro
      - ./02-experiment-tracking:/app/02-experiment-tracking
      - ./03-orchestration:/app/03-orchestration
    command: [
      "jupyter", "lab",
      "--ip", "0.0.0.0",
      "--no-browser"
    ]

  ### MLFLOW ###

  mlflow-postgres:
    image: postgres:15.17-bookworm
    container_name: mlflow-postgres
    environment:
      POSTGRES_USER: ${MLFLOW_POSTGRES_USER}
      POSTGRES_PASSWORD: ${MLFLOW_POSTGRES_PASSWORD}
      POSTGRES_DB: ${MLFLOW_POSTGRES_DB}
    volumes:
      - ./docker/mlflow/postgres/data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${MLFLOW_POSTGRES_USER} -d ${MLFLOW_POSTGRES_DB}"]
      interval: 5s
      timeout: 3s
      retries: 10

  mlflow-minio:
    image: minio/minio:RELEASE.2025-09-07T16-13-09Z
    container_name: mlflow-minio
    environment:
      MINIO_ROOT_USER: ${MLFLOW_MINIO_ROOT_USER}
      MINIO_ROOT_PASSWORD: ${MLFLOW_MINIO_ROOT_PASSWORD}
      MINIO_CONSOLE_ADDRESS: :9001
    ports:
      - "127.0.0.1:9001:9001"
    volumes:
      - ./docker/mlflow/minio/data:/data
    command: [
      "server", "/data"
    ]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:${MLFLOW_MINIO_PORT}/minio/health/live"]
      interval: 5s
      timeout: 3s
      retries: 20

  mlflow-create-bucket:
    image: minio/mc:RELEASE.2025-08-13T08-35-41Z
    container_name: mlflow-create-bucket
    depends_on:
      mlflow-minio:
        condition: service_healthy
    entrypoint:
      - /bin/sh
      - -c
      - |
        mc alias set myminio "http://${MLFLOW_MINIO_HOST}:${MLFLOW_MINIO_PORT}" \
          "${MLFLOW_MINIO_ROOT_USER}" "${MLFLOW_MINIO_ROOT_PASSWORD}"
        mc mb --ignore-existing "myminio/${MLFLOW_MINIO_BUCKET:-mlflow}"
    restart: "no"

  mlflow-server:
    image: ghcr.io/mlflow/mlflow:v3.14.0-full
    container_name: mlflow-server
    depends_on:
      mlflow-postgres:
        condition: service_healthy
      mlflow-minio:
        condition: service_healthy
      mlflow-create-bucket:
        condition: service_completed_successfully
    environment:
      # Backend store URI built from vars
      MLFLOW_BACKEND_STORE_URI: ${MLFLOW_BACKEND_STORE_URI}
      # S3/MinIO settings
      MLFLOW_S3_ENDPOINT_URL: ${MLFLOW_S3_ENDPOINT_URL}
      MLFLOW_ARTIFACTS_DESTINATION: ${MLFLOW_ARTIFACTS_DESTINATION}
      AWS_ACCESS_KEY_ID: ${MLFLOW_MINIO_ROOT_USER}
      AWS_SECRET_ACCESS_KEY: ${MLFLOW_MINIO_ROOT_PASSWORD}
      AWS_DEFAULT_REGION: ${MLFLOW_AWS_DEFAULT_REGION}
      MLFLOW_S3_IGNORE_TLS: "true"
      # Server host/port
      MLFLOW_HOST: ${MLFLOW_HOST}
      MLFLOW_PORT: ${MLFLOW_PORT}
      # Security settings
      MLFLOW_SERVER_ALLOWED_HOSTS: ${MLFLOW_SERVER_ALLOWED_HOSTS}
    ports:
      - "127.0.0.1:${MLFLOW_PORT}:${MLFLOW_PORT}"
    command: [
      "mlflow", "server"
    ]
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:${MLFLOW_PORT}/health')",
        ]
      interval: 10s
      timeout: 5s
      retries: 30

### PREFECT ###

  prefect-postgres:
    image: postgres:15.17-bookworm
    container_name: prefect-postgres
    environment:
      POSTGRES_USER: ${PREFECT_POSTGRES_USER}
      POSTGRES_PASSWORD: ${PREFECT_POSTGRES_PASSWORD}
      POSTGRES_DB: ${PREFECT_POSTGRES_DB}
    volumes:
      - ./docker/prefect/postgres/data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${PREFECT_POSTGRES_USER} -d ${PREFECT_POSTGRES_DB}"]
      interval: 5s
      timeout: 5s
      retries: 5

  prefect-redis:
    image: redis:8.2.7-bookworm
    container_name: prefect-redis
    volumes:
      - ./docker/prefect/redis/data:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  prefect-server:
    image: prefecthq/prefect:3.7.8-python3.14
    container_name: prefect-server
    depends_on:
      prefect-postgres:
        condition: service_healthy
      prefect-redis:
        condition: service_healthy
    environment:
      PREFECT_API_DATABASE_CONNECTION_URL: ${PREFECT_API_DATABASE_CONNECTION_URL}
      PREFECT_SERVER_API_HOST: ${PREFECT_SERVER_API_HOST}
      PREFECT_SERVER_API_PORT: ${PREFECT_SERVER_API_PORT}
      PREFECT_MESSAGING_BROKER: ${PREFECT_MESSAGING_BROKER}
      PREFECT_MESSAGING_CACHE: ${PREFECT_MESSAGING_CACHE}
      PREFECT_REDIS_MESSAGING_HOST: ${PREFECT_REDIS_HOST}
      PREFECT_REDIS_MESSAGING_PORT: ${PREFECT_REDIS_PORT}
      PREFECT_REDIS_MESSAGING_DB: ${PREFECT_REDIS_DB}
      PREFECT_SERVER_ANALYTICS_ENABLED: ${PREFECT_SERVER_ANALYTICS_ENABLED}
    command: prefect server start --no-services
    ports:
      - "127.0.0.1:${PREFECT_SERVER_API_PORT}:${PREFECT_SERVER_API_PORT}"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request as u; u.urlopen('http://localhost:${PREFECT_SERVER_API_PORT}/api/health', timeout=1)"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 60s

  prefect-services:
    image: prefecthq/prefect:3.7.8-python3.14
    container_name: prefect-services
    depends_on:
      prefect-server:
        condition: service_healthy
    environment:
      PREFECT_API_DATABASE_CONNECTION_URL: ${PREFECT_API_DATABASE_CONNECTION_URL}
      PREFECT_MESSAGING_BROKER: ${PREFECT_MESSAGING_BROKER}
      PREFECT_MESSAGING_CACHE: ${PREFECT_MESSAGING_CACHE}
      PREFECT_REDIS_MESSAGING_HOST: ${PREFECT_REDIS_HOST}
      PREFECT_REDIS_MESSAGING_PORT: ${PREFECT_REDIS_PORT}
      PREFECT_REDIS_MESSAGING_DB: ${PREFECT_REDIS_DB}
    command: prefect server services start

  # prefect-worker:
  #   image: prefecthq/prefect:3.7.8-python3.14
  #   container_name: prefect-worker
  #   depends_on:
  #     prefect-server:
  #       condition: service_healthy
  #   environment:
  #     PREFECT_API_URL: ${PREFECT_API_URL}
  #   command: prefect worker start --pool local-pool --type process
  #   restart: on-failure
